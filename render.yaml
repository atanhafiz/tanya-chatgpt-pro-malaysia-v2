services:
  - type: web
    name: tanya-chatgpt-pro-malaysia
    env: docker
    plan: free
    region: singapore
    rootDir: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 3000
